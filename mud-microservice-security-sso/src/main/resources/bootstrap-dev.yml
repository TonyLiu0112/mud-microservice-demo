server:
  port: 9999
  context-path: /security
spring:
  application:
    name: mud-microservice-security
  redis:
    host: localhost
    port: 6379
    password: test123456
  cloud:
    consul:
      host: localhost
      port: 8500
      discovery:
        health-check-path: /${server.context-path}/health