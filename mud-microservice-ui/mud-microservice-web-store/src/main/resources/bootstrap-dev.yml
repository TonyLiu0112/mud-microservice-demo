server:
  port: 9090
spring:
  application:
    name: mud-microservice-web-store
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      discovery:
        enabled: true
        service-id: mud-microservice-configcenter