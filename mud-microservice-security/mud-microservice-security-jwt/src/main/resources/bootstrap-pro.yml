server:
  port: 9998
spring:
  application:
    name: mud-microservice-security-api
  cloud:
    consul:
      host: localhost
      port: 8500
      config:
        enabled: true
        format: yaml
        prefix: mud_configuration/master
        defaultContext: mud-microservice-jwt
        data-key: application-pro.yml