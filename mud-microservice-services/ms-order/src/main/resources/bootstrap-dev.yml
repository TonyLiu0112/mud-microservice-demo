server:
  port: 8081
spring:
  application:
    name: ms-order
  cloud:
    consul:
      host: localhost
      port: 8500
    config:
      discovery:
        service-id: mud-microservice-configcenter
        enabled: true
  zipkin:
    base-url: http://localhost:9411
  sleuth:
    sampler:
      percentage: 1